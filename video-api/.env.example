PORT=3001
MINIO_ENDPOINT=minio.racoondevs.com
MINIO_PORT=443
MINIO_USE_SSL=true
MINIO_ACCESS_KEY=your-access-key
MINIO_SECRET_KEY=your-secret-key

# Directory where uploaded files are stored temporarily
UPLOAD_DIR=./uploads

# Directory where HLS files are generated
HLS_OUTPUT_DIR=./hls-data

# Public URL where HLS files are served from (e.g. Nginx)
HLS_PUBLIC_URL=https://videos.racoondevs.com

# Optional path to ffmpeg binary if not in PATH
# FFMPEG_PATH=/usr/bin/ffmpeg
